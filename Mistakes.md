# Mistakes

The live register. Every defect found in this repository gets a dated row here, whether it was
found by review, by a test, or by the number turning out wrong.

This file is **not** a replacement for [tasks/lessons.md](tasks/lessons.md). The two have
different jobs:

- **`Mistakes.md`** is the working queue — one row per finding, with a status. Rows are never
  deleted, only re-statused.
- **`tasks/lessons.md`** is the distilled artefact — a rule written for the next version of us,
  with the mechanism explained, for the findings general enough to earn one.

A finding graduates from here to there when it is fixed *and* the rule generalises. The row
then reads `promoted` and cites the lessons.md section.

Owned by the `critique-agent` (`.claude/agents/critique-agent.md`), but anyone may add a row.

| status | meaning |
|---|---|
| `open` | found, not yet fixed |
| `fixed` | corrected; the rule is too specific to promote |
| `promoted` | corrected, and the durable rule is now in `tasks/lessons.md` |
| `wontfix` | a real limitation, accepted deliberately, reason recorded |

---

## Register

| # | Date | Where | Finding | Status |
|---|---|---|---|---|
| 1 | 2026-08-28 | `orbit/channel.py::gaseous_loss_db` | Gaseous attenuation magnitudes are placeholders, flagged at runtime. They must be read off ITU-R P.676 before any published figure contains them. No figure currently does, which is why this is open rather than urgent. **Closed 2026-09-20.** Read off ITU-R P.676-12 Annex 2 (standard atmosphere; verified against the `itur` package, two independent code paths agree exactly): 0.0342 dB zenith at 2 GHz, 0.2354 dB at 28 GHz. The 2 GHz placeholder (0.035) was 2% high; the 28 GHz one (0.30) was 27% high. Figure 1 regenerated for both bands, 44 tests pass. | `closed` |
| 2 | 2026-08-28 | `antenna/` | Beam squint across the pass is not modelled. Deferred deliberately: open-loop pointing error already dominates it by an order of magnitude (lessons.md 6.1), so it is not on the critical path to Figure 0. Revisit if a result starts depending on wideband behaviour. | `wontfix` |
| 3 | 2026-09-05 | `satnogs/doppler_fit.py::track_carrier` | First day-3 run reported a residual RMS of 262 to 502 Hz and it was briefly read as a model error. It was the tracker: a 10 dB SNR gate let noise-bin picks through, and on two captures a second strong signal in the band was being tracked instead of the beacon. Fixed by raising the gate to 15 dB and adding `select_carrier`, whose discriminator is physical — the station corrected Doppler for this satellite and nothing else, so the beacon is the only line stationary in frequency. RMS fell to 14.4 and 39.1 Hz. | `promoted` (lessons.md 4.3, already stated; this is its second bill) |
| 4 | 2026-09-05 | `satnogs/doppler_fit.py::decompose_residual` | The three-term fit had no linear-in-time term, so a beacon oscillator warming through the pass came back as a **+5129 ppm Doppler-scale error** on KKS-1 — half a percent of disagreement between two SGP4 implementations on the same TLE, which would have been a serious and wrong claim, and it would have been believed because it was small enough to look plausible. Caught by fitting a plain linear drift instead and finding it explained the same residual better (6.7 Hz unexplained against 13.3 Hz). Fixed by fitting both; the scale then collapses to -680 ppm. | `promoted` (lessons.md 5.5) |
| 5 | 2026-09-05 | `satnogs/captures/14911409`, `14911418` | Both `berlin_ma_sat` captures carry a second signal this tracker cannot separate. Their residual RMS scales linearly with the carrier-selection window (26 Hz at +/-150 Hz, 124 Hz at +/-400 Hz), so any number quoted would be a fact about the window. Reported as rejected, with the window-ratio test now automatic in `doppler_fit`. A tracker that follows a sweeping interferer as a second track would recover them. | `wontfix` |
| 6 | 2026-09-06 | `satnogs/doppler_fit.py::analyse`, `README.md`, `tasks/lessons.md` 8.1 | **The day-3 headline is insensitive to the model it claims to validate.** `rms_about_mean_hz` is `std(f_audio)` — the standard deviation of the tracked audio carrier — and the day-1 prediction never enters it. Verified adversarially: multiplying `orbit.channel.doppler_hz` by 1.10, by 0.50, and by **-1.00** leaves both headline numbers bit-identical (14.367 / 39.064 Hz) *and* leaves `rms_unexplained_hz` bit-identical (3.966 / 6.527 Hz), because the four-column basis `{1, dop, rate, t}` spans the same subspace under any rescaling of `dop`. Not one reported day-3 number would move if the day-1 model were wrong by a factor of two or had the wrong sign. The README's "the model holds against a satellite this lab did not record" and "every Hz of structure in it is real disagreement between two independent implementations" are therefore unsupported by the numbers quoted. What the data does support is an **upper bound**: the lab-vs-station disagreement is at most the total residual, 39 Hz RMS, because disagreement is one unresolved component of it. Reword to the bound, and add a regression test that perturbs `doppler_hz` and asserts some reported quantity moves — today none does. This is lessons.md 5.1 in its strongest form: the test sits at the null, and the null is where the answer is free. | `promoted` (lessons.md 5.7) — 2026-09-06: claim withdrawn from README, todo.md and lessons.md 8.1; `analyse` docstring states what the residual does and does not test; `tests/test_satnogs.py::test_the_reported_rms_does_not_test_the_doppler_model` pins the invariance. |
| 7 | 2026-09-06 | `data/doppler_validation.json` (SEEDS 14917886), `tasks/todo.md` day-3 prediction 4 | **The pathology lesson 5.5 says was fixed is still present on the other accepted capture, and no prose mentions it.** SEEDS returns `scale_fractional = -0.006309`, i.e. **-6309 +/- 136 ppm** — larger in magnitude than the +5129 ppm that lessons.md 5.5 calls "a serious and wrong claim", and it survives *after* the drift term was added. todo.md's verdict on prediction 4 reads "**Holds.** ... With the drift term present the scale is small" and cites only KKS-1's -680 ppm. The two captures give -6309 and -680 ppm with non-overlapping error bars; that disagreement is itself the evidence that `a1` is not measuring a property of the model, and it is the interesting sentence day 3 did not write. Quote both scales, or state the scale is unreportable on both. | `fixed` — 2026-09-06: todo.md prediction 4 re-graded **Fails**; both scales now quoted (SEEDS -6329 ppm, KKS-1 -668 ppm) with the disagreement named as the evidence that `a1` is not a model property. Figure 4's fit legend now names the scale term and marks it not separable. |
| 8 | 2026-09-06 | `satnogs/doppler_fit.py::decompose_residual` (SEEDS) | **SEEDS's 4.0 Hz "unexplained" is bought by two mutually cancelling ~150 Hz terms.** Over the 234 s tracked span `a1*doppler` swings 119 Hz and `a3*t` swings 170 Hz, summing to 54 Hz against a 14.4 Hz residual; the estimator correlation `corr(a1_hat, a3_hat)` is **+0.970**. Fitting drift without scale (`1+rate+t`) leaves 9.20 Hz, so the -6309 ppm scale is doing half the explaining. README and todo.md both describe the unexplained figure as what remains "once the beacon's own oscillator drift is accounted for"; for SEEDS that attribution is false. (KKS-1 is clean on this point: 6.657 Hz drift-only against 6.527 Hz with scale — the scale adds nothing.) Report the drift-only fit alongside, or drop the `a1` column entirely on captures where `scale_separable` is False, rather than quoting a residual that a non-separable term helped shrink. | `fixed` — 2026-09-06: `decompose_residual` also returns `rms_unexplained_drift_only_hz`, reported alongside. SEEDS 4.0 Hz four-term against 9.2 Hz drift-only; KKS-1 6.5 against 6.7. |
| 9 | 2026-09-06 | `satnogs/doppler_fit.py` `WINDOW_NARROW_HZ`, `WELL_POSED_MAX_RATIO` | **The well-posedness test is a null test on the captures it accepts, and the verdict moves with the narrow-window knob.** Both accepted captures return `window_rms_ratio` of exactly 1.000 because `select_carrier` at +/-150 Hz drops 0 of 497 frames on SEEDS and 1 of 1173 on KKS-1 — neither window cuts anything, so the two-point "sweep" carries no information about them. Swept properly, KKS-1's ratio is 1.000 (+/-150 Hz), 1.100 (+/-125), 1.331 (+/-100), 1.483 (+/-80), **1.729 (+/-60, which the 1.5 threshold rejects)**. `WINDOW_NARROW_HZ = 150.0` and `WELL_POSED_MAX_RATIO = 1.5` are two unmeasured point values and KKS-1's acceptance depends on both — lessons.md 5.6 applied to the test that exists to enforce lessons.md 5.6. Mitigating, and it is why this is a defect in the reporting rather than in the verdict: the two rejected captures have `window_fill_fraction` 0.998 and 0.990 against 0.133 and 0.348 for the accepted pair, so *their* rejection is robust to any window. Report the ratio-against-narrow-window curve, or gate on `window_fill_fraction`, which separates the four captures by a factor of three with no threshold to argue about. | `promoted` (lessons.md 5.7, second half) — 2026-09-06: `well_posed` now also requires `window_fill_fraction < WINDOW_SATURATED_FILL` (0.75). Measured 0.13/0.35 accepted against 1.00/1.00 rejected. The ratio test is kept as a second gate and reported. The untested gap between 0.35 and 0.99 is stated at the constant. |
| 10 | 2026-09-06 | `satnogs/doppler_fit.py::track_carrier` `snr_gate_db`, `n_fft` | **Two selection knobs that were never swept move the headline by 2.7x.** KKS-1 residual RMS against the SNR gate: 43.4 Hz at 10 dB, 39.1 at 15, 35.4 at 17, 28.7 at 20, **16.0 at 25 dB**. Against frame length: 32.0 Hz at 4096, 37.8 at 8192, 39.1 at 16384, 41.4 at 32768. `snr_gate_db = 15.0` was chosen while chasing the tracker bug in row 3 — set when the answer was wrong, kept when it looked right — and lessons.md 5.6 says every selection parameter is swept before its result is quoted. The carrier-selection window was swept; the gate and the frame length were not, and on the accepted captures they move the number while the window moves it not at all. SEEDS is stable across the same gate range (13.3 to 15.1 Hz), so this is a KKS-1 finding, and KKS-1 is the capture the headline percentage comes from. Sweep and report, or quote the number as a range. | `fixed` — 2026-09-06: `analyse` takes `snr_gate_db`, `main` sweeps `SNR_GATE_SWEEP_DB` = 10/15/20/25 dB and prints the range. SEEDS 15.1-13.3 Hz, KKS-1 43.4-16.0 Hz. README and todo.md now quote ranges. `n_fft` remains unswept — open, and narrower than this row. |
| 11 | 2026-09-06 | `tests/test_satnogs.py`, `orbit/geometry.py::tle_pass` | **The rejection decision, the audio alignment and the new explicit-window branch have no test.** 42 tests pass and `grep -rn 'epoch_utc\|t_start_utc\|well_posed\|window_rms_ratio\|WELL_POSED\|analyse' tests/` returns nothing. `tle_pass` has no test at all, before or after a 76-line refactor, and it is the function that produces the day-3 prediction. The module docstring's reason — "a test that needs a download is a test that stops running" — is right about the captures and wrong as a conclusion: a synthetic detection population that fills the window would exercise `well_posed` without any download, and a synthetic TLE would exercise the window branch. Lessons.md 4.3 was billed a second time on day 3 and the harness for the *decision* is still unproven. | `open` (partly addressed) — 2026-09-06: the rejection decision now has a synthetic test (`test_a_contaminated_band_saturates_its_selection_window_and_a_clean_one_does_not`). `tle_pass`, `epoch_utc` and the explicit-window branch still have no test. |
| 12 | 2026-09-06 | `orbit/geometry.py:291` | `epoch_utc=tt[i0].utc_iso()` — skyfield's `utc_iso` defaults to `places=0`, so the field the dataclass documents as "Absolute UTC of `t_s` = 0" is **rounded to the nearest whole second**. Measured against the exact `i0 * dt_s`: -0.3 s on SEEDS, +0.4 s on KKS-1. Day 3 happens to be insensitive — a timeline shift `tau` in `d_pred` is absorbed as `a2 -> a2 - a1*tau`, so the fitted shift moves by only 0.002 s — but the sub-0.1 s time shifts todo.md quotes and grades prediction 3 against sit well inside the quantisation, and the next consumer of this field will not be so lucky. One argument: `utc_iso(places=6)`. | `fixed` — 2026-09-06: `utc_iso(places=6)`. The day-3 drift and scale figures moved in the fourth digit as a result (-0.727 to -0.729 Hz/s on SEEDS), which is the size the row predicted. |
| 13 | 2026-09-06 | `satnogs/doppler_fit.py::main` | The reported percentage divides one capture's RMS by **another capture's** peak Doppler: `peak = max(predicted_peak_doppler_hz for good)` and then `100*max(rms)/peak`. Both maxima come from KKS-1 today, so 0.39 % and 0.066 % are arithmetically correct, but the construction is a cross-capture ratio that will silently misreport as soon as the worst RMS and the largest peak Doppler come from different passes. Compute per capture, quote the worst. | `fixed` — 2026-09-06: `main` computes the percentage per capture against that capture's own peak Doppler. |
| 14 | 2026-09-06 | `satnogs/doppler_fit.py::analyse`, `satnogs/manifest.json` | Two small honesty gaps in the same function. (a) The comment "A tuning offset the station recorded on purpose is a known constant... subtract it before attributing anything to the beacon" describes an action the code does not take: `tuned_off_hz` is only reported as `station_tuning_offset_hz`, so KKS-1's `offset_hz` of 1591.9 Hz still has 2000 Hz of known station tuning inside a field the module docstring calls "beat-note offset plus the beacon's own frequency error". The manifest note makes the same promise. Harmless to the headline, because the residual is de-meaned. (b) `analyse` overwrites `decompose_residual`'s fitted `a0` with `f_audio.mean()`, so the field named `offset_hz` in the JSON is not the `a0` the docstring documents and not the quantity `test_decomposition_separates_a_planted_offset_from_a_planted_time_shift` checks. Give the two quantities two names. | `fixed` — 2026-09-06: `offset_hz` is once again the fit's own `a0`; the mean carrier is `carrier_mean_hz` and the tuning-corrected constant is `beacon_offset_hz`. |
| 15 | 2026-09-06 | `satnogs/doppler_fit.py::figure_4` | Figure 4's green curve plots `a1*doppler + a2*rate + a3*t` under the legend "fit: {drift} Hz/s drift, {shift} s shift". On SEEDS the unlabelled `a1` term swings 119 Hz across the pass — larger than either labelled term's visible contribution. A reader takes the curve for drift plus shift. Name the third term or drop it from the plotted curve. | `fixed` — 2026-09-06: the legend names all three plotted terms and appends "(not separable)" when `scale_separable` is False. |
| 16 | 2026-09-06 | day-3 diff `b754bdd..HEAD`, reviewed | Reviewed adversarially on 2026-09-06 and **these held**, recorded because a clean result is evidence too. (a) Lessons.md 4.4 is satisfied: `git show c54689a:tasks/todo.md` carries all five predictions and that commit adds no `doppler_fit.py` — the prediction really was written first. (b) Every number in README.md and todo.md reproduces exactly from `data/doppler_validation.json`; 39.064/9941.4 = 0.393 % and 6.527/9941.4 = 0.0657 %. No transcription. (c) `select_carrier`'s local median continuity filter does **not** absorb real structure on the accepted captures — it drops 0 of 497 frames on SEEDS and 1 of 1173 on KKS-1, and the RMS is unchanged across `floor_hz` 5 to 50, `k_mad` 3 to 5 and `med_win` 9 to 51. (d) No test plants an answer the fit gets for free: the tone sits at 1234.5 Hz off a bin centre, the time shift is parametrised at +/-0.5 and +/-2.0 s away from the degenerate zero, and the genuine-scale-error test uses a 140 s window short enough that the terms actually separate. The day-3 gap is what is not tested (row 11), not what is. | `fixed` |

---

## Findings already promoted

These were found and fixed before this register existed. They live in `tasks/lessons.md` in
full and are listed here so the history is complete.

| Date | Finding | Lesson |
|---|---|---|
| 2026-08-26 | Double conjugation steered the beam to `-theta`; survived review because boresight is degenerate. | 5.1 |
| 2026-08-26 | Interference model gave every neighbour full EIRP — not conservative, a different system. | 5.2 |
| 2026-08-25 | `array/` shadowed Python's stdlib `array` module. | 5.3 |
| 2026-08-26 | `response_db` allocated 615 MB in one go for the aperture-matched Ka array. | 5.4 |
| 2026-08-25 | The plan's Doppler anchors (48 kHz, 640 Hz/s) were wrong; the model corrected them to 45.40 kHz and 581.0 Hz/s. | 1.1 |
| 2026-08-28 | The plan assumed srsRAN Project implements no Rel-17 NTN feature. It implements all three named, plus SIB19 scheduling. | 7.1 |
